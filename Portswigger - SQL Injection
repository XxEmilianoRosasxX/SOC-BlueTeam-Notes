I completed the learning path before creating this repo, all my notes are on discord, aswell as the answers of all labs.

Some of them were a bit buggy, that's the reason i skiped like 3 of them, but i understand how to attack a page that
has an SQL vulnerability, such as the URL contaning the query (UNION attacks), or the cookie manipulation.

You can prevent most instances of SQL injection using parameterized queries instead of string concatenation within the query. These parameterized queries are also known as "prepared statements".

The following code is vulnerable to SQL injection because the user input is concatenated directly into the query:
------------------------------------------------------
String query = "SELECT * FROM products WHERE category = '"+ input + "'";
Statement statement = connection.createStatement();
ResultSet resultSet = statement.executeQuery(query);
----------------------------------------------------
You can rewrite this code in a way that prevents the user input from interfering with the query structure:
PreparedStatement statement = connection.prepareStatement("SELECT * FROM products WHERE category = ?");
statement.setString(1, input);
ResultSet resultSet = statement.executeQuery();

+-------------------------------------------------+
Personal Conclusions
+-------------------------------------------------+
Since it’s a prepared statement, it’s a locked query—you’re limited to searching only one specific table, because the ? expects text (i.e., ‘text’), and if you try to create a payload with ‘UNION SELECT FROM,’ it will return an empty result since it searches for a table with that name, so your data is protected
